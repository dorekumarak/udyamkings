<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= typeof description !== 'undefined' ? description : 'UdyamKings - Empowering Entrepreneurs with Funding and Support' %>">
  <title><%= title || 'UdyamKings - Empowering Entrepreneurs' %></title>
  
  <!-- Preload critical resources -->
  <link rel="preload" href="/css/styles.css" as="style">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Merriweather:wght@400;700&display=swap" 
        rel="stylesheet">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="/css/fontawesome.min.css">
        
  <!-- PWA Manifest -->
  <link rel="manifest" href="/manifest.json">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="theme-color" content="#0A2463">
  
  <!-- Main CSS -->
  <link rel="stylesheet" href="/css/styles.css?v=<%= Date.now() %>">
  
  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/images/udyamkings-logo.png.png">
  
  <!-- Open Graph / Social Media Meta Tags -->
  <meta property="og:title" content="<%= typeof title !== 'undefined' ? title : 'UdyamKings - Empowering Entrepreneurs' %>">
  <meta property="og:description" content="<%= typeof description !== 'undefined' ? description : 'UdyamKings - Empowering Entrepreneurs with Funding and Support' %>">
  <meta property="og:image" content="<%= typeof fullUrl !== 'undefined' ? fullUrl : 'https://udyamkings.com' %>/images/og-image.jpg">
  <meta property="og:url" content="<%= typeof fullUrl !== 'undefined' ? fullUrl : 'https://udyamkings.com' %>">
  <meta property="og:type" content="website">
  
  <!-- Twitter Card Meta Tags -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="<%= typeof title !== 'undefined' ? title : 'UdyamKings - Empowering Entrepreneurs' %>">
  <meta name="twitter:description" content="<%= typeof description !== 'undefined' ? description : 'UdyamKings - Empowering Entrepreneurs with Funding and Support' %>">
  <meta name="twitter:image" content="<%= typeof fullUrl !== 'undefined' ? fullUrl + '/images/og-image.jpg' : '/images/og-image.jpg' %>">
  
  <!-- Canonical URL -->
  <link rel="canonical" href="<%= typeof fullUrl !== 'undefined' ? fullUrl : 'https://udyamkings.com' %><%= typeof url !== 'undefined' && url.pathname ? url.pathname : '' %>">
  
  <!-- Theme Color for Mobile Browsers -->
  <meta name="theme-color" content="#0A2463">
  
  <!-- iOS Meta Tags -->
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="UdyamKings">
  
  <!-- PWA Manifest -->
  <link rel="manifest" href="/manifest.json">
  
  <!-- Critical CSS -->
  <style>
    /* stylelint-disable at-rule-no-unknown */
    /* Critical CSS */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    .animate-fade-in {
      animation: fadeIn 0.5s ease-out forwards;
    }
    
    /* Custom scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
      height: 8px;
    }
    
    ::-webkit-scrollbar-track {
      background: var(--bg-light);
    }
    
    ::-webkit-scrollbar-thumb {
      background: var(--gold);
      border-radius: 4px;
    }
    
    ::-webkit-scrollbar-thumb:hover {
      background: #c19b2e;
    }
    
    /* Critical layout styles */
    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    
    main {
      flex: 1;
      padding-top: var(--header-height);
    }
  </style>
    <link rel="stylesheet" href="/css/styles.css">
    
    <!-- Favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="/images/udyamkings-logo.png.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/udyamkings-logo.png.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/udyamkings-logo.png.png">
    <link rel="manifest" href="/images/favicon/site.webmanifest">
    <meta name="theme-color" content="#0ea5e9">
    
    <!-- Preload critical CSS -->
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fadeIn 0.5s ease-out forwards;
        }
        
        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }
        
        ::-webkit-scrollbar-thumb {
            background: #0ea5e9;
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: #0284c7;
        }
        
        /* Custom animations */
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }
        
        .animate-float {
            animation: float 6s ease-in-out infinite;
        }
        
        /* Gradient text */
        .gradient-text {
            background: linear-gradient(90deg, #0ea5e9, #0d9488);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        /* stylelint-disable at-rule-no-unknown */
        /* Custom button styles */
        .btn-primary {
            background: linear-gradient(to right, #0ea5e9, #0284c7);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 500;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
            transform: translateY(0);
            display: inline-block;
            text-align: center;
            border: none;
            cursor: pointer;
        }
        
        .btn-primary:hover {
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            transform: translateY(-0.125rem);
            opacity: 0.9;
        }
        
        .btn-secondary {
            background: linear-gradient(to right, #0d9488, #0f766e);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 500;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
            transform: translateY(0);
            display: inline-block;
            text-align: center;
            border: none;
            cursor: pointer;
        }
        
        .btn-secondary:hover {
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            transform: translateY(-0.125rem);
            opacity: 0.9;
        }
        
        .btn-outline {
            border: 2px solid #0ea5e9;
            color: #0ea5e9;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 500;
            background-color: transparent;
            transition: background-color 0.2s ease, color 0.2s ease;
            display: inline-block;
            text-align: center;
            cursor: pointer;
        }
        
        .btn-outline:hover {
            background-color: rgba(14, 165, 233, 0.1);
        }
    </style>
  <meta property="og:title" content="<%= title || 'UdyamKings - Turning Ambition Into Empires' %>">
  <% if (typeof description !== 'undefined') { %>
    <meta property="og:description" content="<%= description %>">
  <% } else { %>
    <meta property="og:description" content="Empowering entrepreneurs with funding, mentorship, and resources to build successful businesses.">
  <% } %>
  <meta property="og:image" content="/images/og-image.jpg">
  <meta property="og:url" content="<%= typeof appUrl !== 'undefined' ? appUrl : 'https://udyamkings.com' %>">
  <meta property="og:type" content="website">
  
  <!-- Preload critical resources -->
  <link rel="preload" href="/css/styles.css" as="style">
  <link rel="preload" href="/css/fontawesome.min.css" as="style">
  
  <!-- Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "UdyamKings",
    "url": "<%= typeof appUrl !== 'undefined' ? appUrl : 'https://udyamkings.com' %>",
    "logo": "<%= typeof appUrl !== 'undefined' ? appUrl : 'https://udyamkings.com' %>/images/logo.png",
    "description": "Empowering entrepreneurs with funding, mentorship, and resources to build successful businesses.",
    "sameAs": [
      "https://www.facebook.com/udyamkings",
      "https://twitter.com/udyamkings",
      "https://www.linkedin.com/company/udyamkings"
    ]
  }
  </script>
</head>
<body>
  <!-- Skip to main content for accessibility -->
  <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:bg-white focus:px-4 focus:py-2 focus:z-50 focus:rounded focus:shadow-lg focus:ring-2 focus:ring-gold">
    Skip to main content
  </a>
  
  <!-- Header -->
  <%- include('../partials/header') %>

  <!-- Loading Spinner -->
  <div id="loading" class="fixed inset-0 bg-white bg-opacity-80 flex items-center justify-center z-50 transition-opacity duration-300">
    <div class="animate-spin rounded-full h-16 w-16 border-t-4 border-primary-600 border-opacity-75"></div>
  </div>

  <!-- Main Content -->
  <main id="main-content" class="flex-grow">
    <div class="animate-fade-in">
      <%- body %>
    </div>
  </main>
  
  <!-- Footer -->
  <%- include('../partials/footer') %>
  
  <!-- Back to Top Button -->
  <button id="back-to-top" class="fixed bottom-8 right-8 bg-primary-600 hover:bg-primary-700 text-white p-3 rounded-full shadow-lg transform transition-all duration-300 opacity-0 invisible hover:scale-110">
    <i class="fas fa-arrow-up"></i>
  </button>
  
  <!-- Scripts -->
  <!-- Main JavaScript -->
  <script src="/js/main.js" defer></script>
  
  <!-- Service Worker Registration - Disabled for development -->
  <script>
    // Wait for the DOM to be fully loaded
    document.addEventListener('DOMContentLoaded', () => {
      /* Service Worker registration disabled for development
      if ('serviceWorker' in navigator) {
        navigator.serviceWorker.register('/sw.js')
          .then(registration => {
            console.log('ServiceWorker registration successful');
          })
          .catch(err => {
            console.log('ServiceWorker registration failed: ', err);
          });
      }
      */

      // Mobile menu functionality
      const mobileMenuButton = document.getElementById('mobile-menu-button');
      const mobileMenu = document.getElementById('mobile-menu');
      
      if (mobileMenuButton && mobileMenu) {
        mobileMenuButton.addEventListener('click', () => {
          const isExpanded = mobileMenuButton.getAttribute('aria-expanded') === 'true' || false;
          mobileMenuButton.setAttribute('aria-expanded', !isExpanded);
          mobileMenu.classList.toggle('hidden');
          
          // Toggle menu icon
          const menuIcon = mobileMenuButton.querySelector('svg');
          if (menuIcon) {
            menuIcon.innerHTML = isExpanded 
              ? '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />'
              : '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />';
          }
        });
        
        // Close mobile menu when clicking outside
        document.addEventListener('click', (e) => {
          if (!mobileMenu.contains(e.target) && !mobileMenuButton.contains(e.target)) {
            mobileMenu.classList.add('hidden');
            mobileMenuButton.setAttribute('aria-expanded', 'false');
            const menuIcon = mobileMenuButton.querySelector('svg');
            if (menuIcon) {
              menuIcon.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />';
            }
          }
        });
      }

      // Back to top button functionality
      const backToTopButton = document.getElementById('back-to-top');
      
      if (backToTopButton) {
        window.addEventListener('scroll', () => {
          if (window.pageYOffset > 300) {
            backToTopButton.classList.remove('opacity-0', 'invisible');
            backToTopButton.classList.add('opacity-100', 'visible');
          } else {
            backToTopButton.classList.add('opacity-0', 'invisible');
            backToTopButton.classList.remove('opacity-100', 'visible');
          }
        });

        backToTopButton.addEventListener('click', (e) => {
          e.preventDefault();
          window.scrollTo({ top: 0, behavior: 'smooth' });
        });
      }
    });

  </script>

  <!-- Force remove loader and unregister service workers -->
  <script>
    // Remove loading overlay immediately
    const loadingElement = document.getElementById('loading');
    if (loadingElement) {
      loadingElement.remove();
    }
    
    // Remove any blur effects
    document.body.style.filter = 'none';
    document.body.style.webkitFilter = 'none';
    document.body.style.backdropFilter = 'none';
    
    // Unregister service workers
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.getRegistrations().then(registrations => {
        registrations.forEach(registration => registration.unregister());
      });
    }
  </script>
</body>
</html>
